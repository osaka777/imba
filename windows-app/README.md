# Imba — Windows

Официальная оболочка для Windows (Electron).

## Сборка установщика

```bash
cd /root/onex/windows-app
npm install
npm run dist
```

Артефакт: `dist/imba-bet-windows-setup.exe` → `frontend/public/imba-bet-windows-setup.exe`

Страница: https://imba.bet/windows
